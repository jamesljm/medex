
<hr>
<p></p>

<table class="table table-hover">
    <thead>
    <tr>
        <th>Date</th>
        <th>Schedule</th>
        <th>Doctor Name</th>
        <th>Payment Status</th>
    </tr>
    </thead>
    <tbody>
    <% @bookings.each do |booking| %>
        <tr>
            <td>
                <%= booking.date %>
            </td>
            <td>
                <%= booking.start_time %>
            </td>
            <td>
                <%= link_to t(booking.doctor.first_name + booking.doctor.last_name),  card_doctor_path(booking.doctor.id),remote: true %>
            </td>
            <td>
                <% if booking.bill == 1 %>
                    <button class="btn btn-success" disabled>Paid</button>
                <% else %>
                    <%= link_to "Make Payment", payment_pages_path(:booking_id => booking.id), remote: true, class: "btn btn-primary" %>
                <% end %>
            </td>
            <td>
                <%= link_to 'Delete', booking_path(booking), remote: true,
                    class: 'btn btn-sm btn-danger',
                    method: :Delete, 
                    data: {confirm: 'Are you sure?'},
                    remote: true %>
            </td>
        </tr>
        <% end %>
    </tbody>
</table>
<br>
